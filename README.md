# App_React_JET-Solutions

- clone branch `master` with
  `git clone git@github.com:ivanberko/AppReactJETSolutions.git` or fork it and then
  clone it from your forked repo
- `cd App_React_JET-Solutions`
- `npm install`
- `npm start`